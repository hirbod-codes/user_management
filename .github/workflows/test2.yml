on:
    pull_request:
        branches:
            - main
    workflow_dispatch:

jobs:
    create-release-version:
        name: Create release version
        runs-on: ubuntu-latest
        permissions:
            packages: write
            contents: write
            pull-requests: write

        steps:
            - name: Checkout
              uses: actions/checkout@v2
              with:
                fetch-depth: 0

            - name: Changelog
              uses: scottbrenner/generate-changelog-action@master
              id: changelog
              env:
                REPO: ${{ github.repository }}

            - name: Build Changelog
              id: github_release
              uses: mikepenz/release-changelog-builder-action@v3
              env:
                GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

            - name: Bump version and push tag
              id: tag_version
              uses: mathieudutour/github-tag-action@v6.1
              with:
                github_token: ${{ secrets.GITHUB_TOKEN }}

            - name: Create a GitHub release
              uses: ncipollo/release-action@v1
              with:
                tag: ${{ steps.tag_version.outputs.new_tag }}
                name: Release ${{ steps.tag_version.outputs.new_tag }}
                body: ${{ steps.github_release.outputs.changelog }}

            - run: echo "${{steps.tag_version.outputs.new_tag}}";
            - run: echo "${{steps.tag_version.outputs.new_version}}";
            - run: echo "${{steps.tag_version.outputs.previous_tag}}";
            - run: echo "${{steps.tag_version.outputs.release_type}}";
            - run: echo "${{steps.tag_version.outputs.changelog}}";
